/**
 * The routes that are publicy accessible to anyone
 * @type {string[]}
 */
export const publicRoutes = ["/", "/mentors"];

/**
 * The routes that are used to authentica users
 * @type {string[]}
 */
export const authRoutes = ["/auth"];
